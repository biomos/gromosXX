gromos:ubuntu-20.04-mpi:configure:
  extends:
    - .gromos:base:configure
    - .use-mpi
    - .use-share
    - .use-debug
  image: ${CI_REGISTRY}/gromos/gromosxx/gromos-ubuntu-20.04-mpi
  variables:
    BUILD_DIR: build

gromos:ubuntu-20.04-mpi:build:
  extends:
    - .gromos:base:build
    - .use-mpi
  image: ${CI_REGISTRY}/gromos/gromosxx/gromos-ubuntu-20.04-mpi
  variables:
    BUILD_DIR: build/bin/md_mpi
  needs:
    - job: gromos:ubuntu-20.04-mpi:configure
